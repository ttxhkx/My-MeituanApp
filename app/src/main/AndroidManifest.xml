<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lenovo.mymeituanapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondActivity" />
        <activity android:name=".log" />
        <activity android:name=".CreatShop" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".OthersShopActivity" />
        <activity android:name=".FoodActivity" />
        <activity android:name=".CreatFoodActivity" />
        <activity android:name=".OhersFoodActivity"></activity>
    </application>

</manifest>